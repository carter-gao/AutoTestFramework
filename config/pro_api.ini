[project]
# 测试环境主机地址
# https://www.toutiao.com/; https://www.apiopen.top/
address = https://www.apiopen.top/
# 接口超时设置(单位：s)
timeout = 10
# 代理
proxy =
# pytest测试框架设定本次测试关键词，据此自动获取yaml文件中的用例
# yaml文件名关键词，若不定义则默认读取所有yaml文件，建议不同模块在编写用例时文件名带标识，单独测试此模块时，配置这个标识关键词
# 可配置多个关键词，以英文逗号分隔
filename_keyword =
